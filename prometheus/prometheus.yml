global:
  scrape_interval: 5s # Pull metrics every 5 seconds for fast local testing
  evaluation_interval: 5s

scrape_configs:
  - job_name: "sentinel-flux-backend"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["172.21.245.141:8085"]